const express = require('express')
const router = express.Router()
const Member = require('../models/member')

// @desc    fazer as quatros rotas de CRUD para os membros com base nas regras



module.exports = router